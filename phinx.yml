paths:
    migrations: %%PHINX_CONFIG_DIR%%/migrations

environments:
    default_migration_table: migrations
    default_database: development

    development:
        adapter: sqlite
        name: ./data/schedule.sqlite3

    testing:
        adapter: sqlite
        memory: true
